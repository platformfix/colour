apiVersion: v1
kind: Service
metadata:
  name: {{ include "colour.name" . }}
  labels:
    {{- include "colour.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "colour.labels" . | nindent 4 }}
